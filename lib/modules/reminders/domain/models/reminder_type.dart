/// Factory reminder notification categories.
enum ReminderType {
  /// Recurring expense due reminder.
  recurringExpense,

  /// Maintenance expense reminder.
  maintenance,

  /// Calendar event reminder.
  calendarEvent,

  /// Offline sync failure alert.
  syncFailed,
}
