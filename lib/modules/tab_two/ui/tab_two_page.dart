import '../../../../utils/exports.dart';

/// A page that displays the content for Tab Two.
///
/// This page shows the [TabTwoWidget].
@RoutePage()
class TabTwoPage extends StatelessWidget {
  /// The constructor for [TabTwoPage].
  const TabTwoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const TabTwoWidget();
  }
}
