import '../../../../utils/exports.dart';

/// A page that displays the details for Tab Two.
///
/// This page shows the [TabTwoDetailWidget].
@RoutePage()
class TabTwoDetailPage extends StatelessWidget {
  /// The constructor for [TabTwoDetailPage].
  const TabTwoDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const TabTwoDetailWidget();
  }
}
