rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Attachment files uploaded from mobile and web admin.
    match /attachments/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
